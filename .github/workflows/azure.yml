name: Sincronizar com Azure Repos

on:
  push:
    branches:
      - main
      - develop

jobs:
  sync-with-azure:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout no código
      uses: actions/checkout@v2

    - name: Sincronizar com o Azure Repos
      uses: actions/setup-azure-devops-extension@v0
      with:
        azure-devops-url: 'https://dev.azure.com/desenvolvimento0610/_git/CRM'
        azure-devops-token: '${{ secrets.SYNCGITHUB }}'